<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Center the MedRetailers logo and size it to the safe zone (72dp)
         to ensure it fits perfectly within any adaptive icon mask. -->
    <item
        android:drawable="@drawable/medretailer_logo"
        android:width="72dp"
        android:height="72dp"
        android:gravity="center" />
</layer-list>